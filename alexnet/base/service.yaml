apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: alexnet
  namespace: default
  labels:
    app: alexnet
    serviceType: faas
spec:
  template:
    spec:
      containers:
      - image: ${ALEX_NET_IMAGE}
