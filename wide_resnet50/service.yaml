apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: wide_resnet50_2
  namespace: default
spec:
  template:
    spec:
      containers:
      - image: docker.io/{username}/wide_resnet50_2
